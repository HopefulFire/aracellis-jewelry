<h1>Comments</h1>
<div class="comments">
	<% Comments.all.reverse_each do |comment| %>
		<h2><%= comment.user.username %></h2>
		<p><%= comment.body %></p>
		<% if @user == comment.user %>
			<a href=<%= "/comments/#{comment.id}/edit" %>>Edit</a>
		<% end %>
	<% end %>
</div>