<div class="posts">
	<% if @posts %>
		<% @posts[0..10].each do |post| %>
			<a href=<%= "/posts/#{post.id}" %>>
				<h2><%= post.title %></h2>
				<h3><%= post.user.username %></h3>
				<p><%= post.body[0..100] %>...</p>
			</a>
		<% end %>
	<% end %>
</div>