<div class="users">
	<% if @users && @users != [] %>
		<% @users.each do |user| %>
			<a href=<%= "/users/#{user.id}" %>>
				<h3><%= user.username %></h3>
			</a>
		<% end %>
	<% else %>
		<h3>Nothing here!</h3>
	<% end %>
</div>