<div class="user_home">
	<% if @user %>
		<p>Hi, <%= @user.username %></p>
		<a href=<%= "/users/#{@user.id}"%>>Profile</a>
		<a href="/users/logout">Logout</a>
	<% else %>
		<p>Hi, anonymous user</p>
		<a href="/users/login">
	<% end %>
</div>