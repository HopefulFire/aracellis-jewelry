<div class="user_home">
	<% if @user %>
		<p>Hi, <%= @user.username %></p>
		<a href=<%= "/user/#{@user.id}"%>>Profile</a>
		<a href="/user/logout">Logout</a>
	<% else %>
		<p>Hi, anonymous user</p>
		<a href="/user/login">
	<% end %>
</div>